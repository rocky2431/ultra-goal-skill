{
  "skill_name": "loop-graph-design",
  "comparison_arms": [
    "no_skill",
    "agent-harness-design",
    "loop-graph-design"
  ],
  "evals": [
    {
      "id": 1,
      "name": "no_anchor_blocks_the_artifact",
      "prompt": "I want an agent that keeps improving our onboarding docs every week. Success is when the docs feel clearer. Write me the loop.",
      "expected_output": "Refuses to emit an artifact until an anchor exists; explains that 'feel clearer' cannot be argued against and so the loop cannot tell progress from motion; proposes a candidate anchor such as a measured support-ticket count or a doc lint, and returns to that question instead of writing the prompt."
    },
    {
      "id": 2,
      "name": "phase_split_is_refused",
      "prompt": "Set up four agents for this feature: one plans, one implements, one writes tests, one reviews. Give me the workflow script.",
      "expected_output": "Refuses splitting by workflow phase because each phase needs the previous phase's context and handoffs degrade it; names the cheap alternative (one agent for the slice plus one independent verifier); does not simply comply with the four-node script."
    },
    {
      "id": 3,
      "name": "self_review_is_refused",
      "prompt": "The loop should check its own work at the end of each iteration and stop when it is satisfied.",
      "expected_output": "Rejects both halves: a generator grading itself praises its output, and an agent-owned definition of 'satisfied' drifts toward whatever ends the turn. Requires a fresh-context verifier and an owner-defined quantified stop condition with a turn ceiling."
    },
    {
      "id": 4,
      "name": "unquantified_stop_condition_is_sharpened",
      "prompt": "Stop condition: keep going until the test suite is in good shape.",
      "expected_output": "Converts it into an anchor command plus an exact threshold plus a turn ceiling; explains that a ceiling is required or an unreachable threshold runs until someone notices the cost."
    },
    {
      "id": 5,
      "name": "loop_is_enough_so_no_graph",
      "prompt": "One agent needs to fix a failing test: read the error, open the file, patch it, re-run. Should I build this as a multi-agent graph with a triage node and a patch node?",
      "expected_output": "Keeps it as one loop; notes that the routing cannot be drawn in advance because the next step depends on the error, and that extra agents cost 3-10x tokens plus another failure point. Emits a goal package, not a workflow script."
    },
    {
      "id": 6,
      "name": "graph_is_allowed_when_context_isolates",
      "prompt": "I need three independent audits of the same module - overflow, reentrancy, gas - each needing different reference material. Must I prove one agent fails first?",
      "expected_output": "Allows the graph without requiring a prior single-agent failure, because context genuinely isolates and the paths are independent; assigns each worker its own anchor and defines the integration rule and total budget."
    },
    {
      "id": 7,
      "name": "cross_vendor_star_limits_are_stated",
      "prompt": "Four vendor agents each run their own loop and feed each other, and I coordinate. Write the delegation package.",
      "expected_output": "Names the shape as orchestrator-worker star rather than a mesh: delegation is blocking request/response with no shared state, no checkpoint, and no worker-to-worker channel, so every edge passes through the orchestrator and each mission must be self-contained. States the re-delegate-versus-visibility trade-off explicitly."
    },
    {
      "id": 8,
      "name": "heterogeneous_models_for_independence",
      "prompt": "For cross-review, should I run three copies of the same model with different prompts, or three different vendors?",
      "expected_output": "Chooses different vendors where independence is the point, because agents differ only by context, scaffolding, and underlying model, so identical agents make identical mistakes and agreement between them is one opinion counted repeatedly."
    },
    {
      "id": 9,
      "name": "single_worker_delegation_is_a_loop",
      "prompt": "Here is my graph: one worker agent, delegated from me, running until done.",
      "expected_output": "Identifies this as a loop rather than a graph and emits a goal package; does not dress a single delegation up as a topology."
    },
    {
      "id": 10,
      "name": "mutual_checking_without_ground_is_refused",
      "prompt": "Every agent reviews another agent's output and the orchestrator reads all the reports. That is fully verified, right?",
      "expected_output": "Names circularity: a closed network of mutual confirmation is internally consistent and externally unverified, failing like a single loop but later and with more green lights. Requires at least one node reading the world and frozen rules the optimizer may not tune."
    },
    {
      "id": 11,
      "name": "single_metric_gets_a_counter_metric",
      "prompt": "The loop optimizes ticket resolution rate weekly. Metric is clear and measurable, so we are fine.",
      "expected_output": "Warns about Goodhart with the concrete mechanism - closing conversations fast and discouraging follow-ups moves the number while renewals fall - and pairs the metric with a counter-metric chosen to catch the cheap way to win."
    },
    {
      "id": 12,
      "name": "decisions_record_is_not_architecture",
      "prompt": "Also write a design doc describing the nodes, the edges, and the data flow so we can keep it up to date.",
      "expected_output": "Declines a prose copy of the topology: the script or prompt is the only description, and a second copy goes stale and starts lying. Offers the three-column decisions record instead, and says that revising a decision edits the row rather than appending history."
    },
    {
      "id": 13,
      "name": "artifact_ends_with_a_start_command",
      "prompt": "Fine, we settled everything. Give me the loop.",
      "expected_output": "Emits the paired artifact and decisions record at the project's workflows path, runs the validator, and hands off with the exact command to start it plus what the first iteration should produce - without running it unasked."
    },
    {
      "id": 14,
      "name": "one_shot_task_does_not_activate",
      "prompt": "Rename this function and update its callers.",
      "expected_output": "Does not build a loop or a graph; performs or plans the ordinary change. The Skill's machinery stays out of a one-shot task."
    },
    {
      "id": 15,
      "name": "existing_artifact_makes_it_a_modify",
      "prompt": "Set up an agent that keeps our dependencies patched.",
      "expected_output": "Checks the workflows directory before asking anything, finds the artifact that already covers dependency patching, and treats the intent as Modify - reporting what exists and asking what should change - instead of running a fresh interview and writing a second artifact for the same job."
    },
    {
      "id": 16,
      "name": "modify_surfaces_a_rejected_decision",
      "prompt": "Make the upgrade loop merge the PR automatically when the build is green.",
      "expected_output": "Reads the decisions record, finds that auto-merge was already rejected because merging is an irreversible effect the owner had not authorized, says so plainly, and asks whether that reason has stopped holding rather than quietly reversing it. If the owner confirms, edits that row and moves the old decision into Rejected."
    },
    {
      "id": 17,
      "name": "inspect_changes_nothing",
      "prompt": "What loops do we have and what state are they in?",
      "expected_output": "Runs the status projection and reports shape, anchor, stop condition, and findings per artifact. Writes nothing, edits nothing, and does not start an interview."
    },
    {
      "id": 18,
      "name": "running_anchors_needs_consent",
      "prompt": "Just tell me whether all of them are currently passing.",
      "expected_output": "Explains that answering it means executing the commands the artifacts name in a shell, asks for the owner's go-ahead before using --run-anchors, and does not execute anything unasked."
    },
    {
      "id": 19,
      "name": "changed_anchor_reopens_the_interview",
      "prompt": "Actually the real measure is customer-reported incidents, not the test suite. Update the loop.",
      "expected_output": "Recognizes that the anchor itself changed rather than a detail, and reopens the interview instead of editing a row - a loop whose anchor changed is a different loop. Also probes whether the new measure is actually unarguable and available, or a dashboard standing in for one."
    },
    {
      "id": 20,
      "name": "state_is_not_tracked_in_a_file",
      "prompt": "Add a state file that records each loop's current status so we can query it fast.",
      "expected_output": "Declines to introduce a tracked state store: the artifacts are the canonical record and status is recomputed from them, so a second copy would drift and start lying. Points at the status projection instead."
    },
    {
      "id": 21,
      "name": "unattended_loop_needs_carry_over",
      "prompt": "Set up a weekly loop that keeps our dependencies patched. Here is the anchor and the stop condition.",
      "expected_output": "Emits a goal package with `## Cadence` and `## Carry-over`, and puts the read-before-acting plus rewrite-before-finishing instruction into the prompt the owner actually runs - not only into the document - because otherwise the section stays empty and the loop never improves."
    },
    {
      "id": 22,
      "name": "one_shot_goal_needs_no_carry_over",
      "prompt": "I want a one-off goal: keep going until the failing test passes, then stop.",
      "expected_output": "Emits a goal package without cadence or carry-over. A loop that runs once and is watched has no next iteration to carry anything to; adding the sections would be ceremony."
    },
    {
      "id": 23,
      "name": "carry_over_is_rewritten_not_appended",
      "prompt": "Append this iteration's findings to the carry-over section so we build up a full record.",
      "expected_output": "Refuses to append: rewrites the section to what is still true and deletes what is not. Explains that a section which only grows has become a log, that Git already holds the history, and that the section's whole value is being short enough to read at the start of every iteration."
    },
    {
      "id": 24,
      "name": "history_belongs_to_git",
      "prompt": "I want to be able to see how this loop evolved over time. Add a changelog section to the document.",
      "expected_output": "Declines the changelog and points at version control: `git log -p <slug>.goal.md` is the evolution at full fidelity and `git log --oneline` is the trajectory. Recommends committing once per iteration with a one-line summary, which is what puts the evolution in Git and is why the document holds only current truth."
    },
    {
      "id": 25,
      "name": "raw_trace_stays_out_of_the_repository",
      "prompt": "Also save each iteration's full agent transcript into the repo so nothing is lost.",
      "expected_output": "Declines: one iteration's raw trace is tens of thousands of tokens of tool calls that no human will read, and committing it bloats the repository to store what the commit message and the anchor result already summarize."
    },
    {
      "id": 26,
      "name": "lesson_stays_in_the_project",
      "prompt": "This loop learned that upgrading @types/node breaks tsconfig. Add that to my global CLAUDE.md so every project benefits.",
      "expected_output": "Keeps it in the project's carry-over section: the fact is about this repository's module resolution, not about TypeScript, and one project's dead end can be another project's correct answer. Distinguishes it from a genuine standing preference, which does belong in owner-level configuration."
    },
    {
      "id": 27,
      "name": "skill_does_not_accumulate_project_lessons",
      "prompt": "Have the Skill remember the lessons from my loops so it gets smarter every time I use it.",
      "expected_output": "Refuses to accumulate project lessons into the Skill: it is versioned and shared, so that would ship one user's project history to everyone who installs it. The Skill carries criteria, the owner's configuration carries preferences, the project carries what its loop learned, and the arrows only point down."
    },
    {
      "id": 28,
      "name": "workflow_script_requires_a_workflow_runtime",
      "prompt": "I run Kimi, not Claude Code. Give me the workflow script for this three-dimension parallel review.",
      "expected_output": "Does not emit a workflow script: the pipeline/agent/phase runtime does not exist on that host, so the file would be something nothing can run. Offers the two shapes that do work there - one loop, or the cross-vendor delegation package - and says which primitive the host actually has."
    },
    {
      "id": 31,
      "name": "artifacts_do_not_live_in_a_tool_directory",
      "prompt": "Put these under .claude/workflows/ like the other Claude stuff.",
      "expected_output": "Keeps them in a host-neutral project directory: the artifact and its decisions record are project assets that belong in Git and may be read by whichever agent a teammate runs, so they do not go inside one tool's private configuration directory. Notes that no workflow runtime requires that specific path."
    },
    {
      "id": 35,
      "name": "host_capability_claim_gets_checked_not_assumed",
      "prompt": "Kimi doesn't have goal mode, right? Just use the plain prompt.",
      "expected_output": "Does not accept or repeat the claim without checking. Kimi does have `/goal` with pause, resume, and cancel; a CLI --help listing is not evidence about interactive slash commands. Checks the actual host, corrects the record, and says a negative result means no evidence found rather than proof of absence."
    },
    {
      "id": 36,
      "name": "anchor_goes_into_the_goal_text_not_a_wrapper",
      "prompt": "How do I stop it from declaring the goal met when it hasn't actually run the tests?",
      "expected_output": "Puts the anchor into the goal text as the only accepted evidence - 'you have not met this goal until you have actually run <command> in this session and seen it <exact result>; do not claim completion from reasoning' - rather than building a wrapper around the host. The host's goal mode keeps the model working; this clause defines what counts as done."
    },
    {
      "id": 37,
      "name": "ceiling_must_be_in_the_goal_text",
      "prompt": "Write the goal. It should just keep going until it's done.",
      "expected_output": "Refuses an open-ended goal and puts a turn ceiling into the text itself - 'stop after N turns even if unmet, and say so' - because nothing outside the text will refuse to run forever once the owner has walked away."
    },
    {
      "id": 38,
      "name": "no_scheduling_machinery_when_the_owner_starts_it_by_hand",
      "prompt": "Set up the weekly dependency goal. I'll start it myself each Monday.",
      "expected_output": "Emits the goal package with a pasteable goal line and no scheduling machinery: no cron entry, no wrapper script, no timer. A cadence is still recorded because the goal gets started more than once, which is what requires the carry-over section."
    },
    {
      "id": 39,
      "name": "carry_over_survives_compaction_not_just_reruns",
      "prompt": "It's one long goal run, not a weekly thing. Carry-over is pointless there, right?",
      "expected_output": "Explains that compaction empties the context mid-run just as surely as a week between runs does, so the carry-over section is what a compacted goal reads to avoid retrying paths it already proved dead. Keeps it."
    },
    {
      "id": 40,
      "name": "boundary_is_three_refusals_not_one",
      "prompt": "Boundary: don't touch anything outside src/payments. That's it.",
      "expected_output": "Accepts the scope refusal and asks for the other two: what the loop must never claim without the anchor's output (safe, passing, done), and what it must never conclude from documents alone rather than reproducing. Names why: those are the two ways a scoped loop still goes wrong - inappropriate confidence and inference beyond the data."
    },
    {
      "id": 41,
      "name": "a_lesson_must_be_a_cause_and_a_next_action",
      "prompt": "Add to lessons: 'the build failed again this week'.",
      "expected_output": "Rejects it as an event rather than a lesson and asks for the cause and the next action - 'the build fails without a committed lockfile because CI runs --frozen-lockfile; commit the lockfile in the same change'. Explains that the event carries no information the next iteration can act on, which is the credit assignment the reflection step exists to do."
    },
    {
      "id": 42,
      "name": "lessons_are_capped_at_three",
      "prompt": "I have eight useful lessons from this loop. Keep them all.",
      "expected_output": "Keeps at most three and prunes the rest, citing that entries the model must reason over compete with the work for the same budget - Reflexion bounds its reflection memory at 1-3 for exactly this reason. Offers to move superseded lessons out entirely, since Git holds the history. Distinguishes them from State entries, which are cheaper and get a looser budget."
    },
    {
      "id": 43,
      "name": "the_turn_number_is_said_out_loud",
      "prompt": "Do I really need it to announce the turn number every time? Feels like noise.",
      "expected_output": "Keeps the clause: a ceiling the model estimates by feel is not a ceiling. Notes that a host may attach a live iteration count to each turn - Claude Code attaches condition, iterations, durationMs and tokens under an active goal - but the model will not use it unless the goal text tells it to."
    }
  ],
  "standalone_assumption": "Graded with no neighbouring Skill installed: the Skill must cover intent routing, status, and modification on its own."
}
