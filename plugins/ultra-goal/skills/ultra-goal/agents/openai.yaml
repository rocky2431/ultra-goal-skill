interface:
  display_name: "UltraGoal"
  short_description: "Turn a vague objective into a goal an agent can hold to"
  default_prompt: "Use $ultra-goal to turn an objective into a goal with an anchor, a boundary, and an owner-defined stop condition."
policy:
  allow_implicit_invocation: true
